<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to image_tag('HouseLogoBlue.png', size: "150x108"), root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Agents", agents_path, class: 'menu-items' %></li>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Ratings
            <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li id="dropdown"><%= link_to "Create new rating", new_agent_rating_path(Agent.first) %></li>
            <li id="dropdown"><%= link_to "View all ratings", ratings_path %></li>
            <li id="dropdown"><a href="#">Something else here</a></li>
            <li role="separator" class="divider"></li>
            <li id="dropdown"><a href="#">Separated link</a></li>
            <li role="separator" class="divider"></li>
            <li id="dropdown"><a href="#">One more separated link</a></li>
          </ul>
        </li>

        <% if logged_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Action
                <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li id="dropdown"><%= link_to "New Agent", new_agent_path %></li>
                <li id="dropdown"><a href="#">Another action</a></li>
                <li id="dropdown"><a href="#">Something else here</a></li>
                <li role="separator" class="divider"></li>
                <li id="dropdown"><a href="#">Separated link</a></li>
                <li role="separator" class="divider"></li>
                <li id="dropdown"><a href="#">One more separated link</a></li>
              </ul>
            </li>
        <% end %>
      </ul>
      <form class="navbar-form navbar-left">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
            <% if logged_in? && current_user.admin? %>
                <li><%= link_to "Users", users_path %></li>
            <% end %>
            <li><%= link_to 'Log out', logout_path, method: :delete %></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= "#{current_user.username}" %> <%= "(Admin)" if current_user.admin? %>
                <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li id="dropdown"><%= link_to "Edit your profile", edit_user_path(current_user) %></li>
                <li id="dropdown"><%= link_to "View your profile", user_path(current_user) %></li>
                <li id="dropdown"><a href="#">Another action</a></li>
                <li id="dropdown"><a href="#">Something else here</a></li>
                <li role="separator" class="divider"></li>
                <li id="dropdown"><a href="#">Separated link</a></li>
              </ul>
            </li>
        <% else %>
            <li><%= link_to 'Log in', login_path %></li>
            <li><%= link_to 'Sign up', signup_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>